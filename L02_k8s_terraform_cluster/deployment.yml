apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-echo-app
  namespace: default
  labels:
    app: echo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: echo
  template:
    metadata:
      labels:
        app: echo
    spec:
      containers:
      - name: echo-container
        image: hashicorp/http-echo
        args:
          - "-text=Hallo aus Kubernetes und Go!"
        ports:
        - containerPort: 5678
